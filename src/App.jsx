import React from 'react';
import { AppRoutes } from './routes';

/**
 * The root component of the Paxx application.
 * It serves as the entry point for rendering the entire application's
 * routing and page structure.
 */
function App() {
    return <AppRoutes />;
}

export default App;